# Meet events service

GraphQL API for managing user events.

## Technologies

- Phoenix
- PostgreSQL
- Absinthe

## Start developing

Run `docker-compose up -d`

Run `mix ecto.create`

Run `mix do deps.get, compile`

Run `mix phx.server`